/**
 * @file Type Aliases - Character
 * @module vfile-reader/types/Character
 */

/**
 * Character in a source file, with `null` denoting the end of file.
 */
type Character = string | null

export type { Character as default }
